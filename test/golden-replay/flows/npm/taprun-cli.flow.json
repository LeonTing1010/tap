{
  "id": {
    "site": "npm",
    "name": "taprun-cli"
  },
  "description": "npm registry record for @taprun/cli (golden-replay fixture for Layer 2). Self-referential — if npm registry breaks the @taprun/cli URL, Layer 2 catches it before users do.",
  "observe": [
    {
      "op": "fetch",
      "url": "https://registry.npmjs.org/@taprun/cli",
      "method": "GET",
      "format": "json",
      "save": "body",
      "expect": "has(result.name) && \"dist-tags\" in result && size(result.versions) > 0"
    }
  ],
  "return": "{\"outcome\": dyn(has(observe.body.name) ? \"ok\" : \"unavailable\"), \"reason\": dyn(has(observe.body.name) ? \"\" : \"registry record has no name field\"), \"name\": dyn(observe.body.name), \"latest\": dyn(observe.body[\"dist-tags\"][\"latest\"]), \"version_count\": dyn(size(observe.body.versions))}",
  "must": [
    {
      "moment": "post_return",
      "holds": "result.outcome == \"ok\" && result.version_count > 0"
    }
  ],
  "source_url": "https://registry.npmjs.org/@taprun/cli",
  "source_intent": "npm registry record for @taprun/cli"
}
