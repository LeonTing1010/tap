{
  "id": {
    "site": "github",
    "name": "browserbase-search"
  },
  "observe": [
    { "op": "nav", "url": "https://github.com/browserbase" },
    {
      "op": "eval",
      "fn": "/* TODO: stagehand act(\"click on the stagehand repo\") on line 19 requires an LLM at runtime — v2 has no op:exec, declare correct returns.type and replace this stub with deterministic ops or a real eval body. Original: await stagehand.act(\"click on the stagehand repo\"); */ () => ({})",
      "returns": { "type": "object" }
    },
    {
      "op": "eval",
      "fn": "/* TODO: stagehand extract(\"the author and title of the latest PR\") on line 20 requires an LLM at runtime — v2 has no op:exec, declare correct returns.type and replace this stub with deterministic ops or a real eval body. Original: const { author, title } = await stagehand.extract(\"the author and title of the latest PR\", z.object({ author: z.string(), title: z.string() })); */ () => ({})",
      "returns": { "type": "object" }
    },
    {
      "op": "eval",
      "fn": "/* TODO: page.screenshot() on line 21 has no v2 equivalent (op:screenshot was retired in plan-v2). Either delete or replace with an out-of-band capture. */ () => ({})",
      "returns": { "type": "object" }
    }
  ],
  "return": "true"
}
